---
scope: MVP FM Live Wire — architecture technique mono-process one-way MIDI broadcast
purpose: Spine build-substrate + doc architecture humaine FR + 8 ADR, exploitables pour epics/stories puis code
altitude: feature (garde les epics)
updated: 2026-07-06T11:34
---

- (decision) AD-3 Web MIDI API native, pas WEBMIDI.js MVP. Binds: FR-6..7,15, NFR-6..8. Prevents: dépendance wrapper pour 5 types channel-voice. Rule: requestMIDIAccess({sysex:false}) natif; MIDIInput.onmessage capture; MIDIOutput.send(data,ts) rendu; feature-detection avant prompt.
- (constraint) Invariant produit verrouillé 2026-07-06: one-way broadcast Performer→Serveur→Listeners, owner unique, 2e performer refusé performer:busy, listeners read-only, pas de SysEx MVP, Panic local fonctionne serveur down, HTTPS+Chrome/Edge, mono-process mono-domaine, pas de DB.
- (decision) AD-2 One-way broadcast owner unique. Binds: FR-1..5,19..24. Prevents: listener émettant midi:event accepté / 2e performer prenant la main. Rule: io.use épingle socket.data.role+performerId=socket.id; socket.use gate role+ownerPerformerId sur midi:event→forbidden; PerformerRegistry ownerPerformerId single-slot, 2e→performer:busy.
- (decision) AD-7 Panic local côté listener. Binds: FR-16,17,18,24, S-2. Prevents: note coincée sans issue si serveur down. Rule: panic.ts ne dépend PAS de Socket.IO, uniquement MIDIOutput locale; CC64→120→121→123 ×16=64 msg; Force Panic noteOff sweep 128×16=2048 opt-in confirmé; pas de handler panic serveur.
- (decision) AD-1 Paradigme: monolithe modulaire mono-process (layered + couche Socket dédiée). Binds: all. Prevents: séparation frontend/backend en 2 déploiements → CORS + secure context fragmenté. Rule: un seul process Node porte Express (static+/health) ET Socket.IO sur même origin HTTPS.
- (decision) AD-5 Contrat MIDI partagé Zod dans @fmlw/shared. Binds: FR-10,15,21, NFR-12,15. Prevents: dérive schéma front/back. Rule: source unique MidiEventSchema, importé front+back; .strict()/z.strictObject rejette champs smugglés; v!==1→unsupported-version; performerId interdit payload (serveur attache socket.id).
- (decision) AD-8 Exclusion SysEx MVP (double défense). Binds: FR-8, NFR-20. Prevents: injection firmware/DoS via SysEx non borné. Rule: performer filtre event.data[0]===0xF0 jamais envoyé; schéma Zod n'expose aucun type SysEx→rejet automatique; requestMIDIAccess({sysex:false}).
- (decision) AD-4 Socket.IO v4 pour relay temps réel. Binds: FR-10,11,18, NFR-14. Prevents: reimplémenter rooms/reconnexion/recovery/ack/middlewares sur ws nu. Rule: pin ^4.8.3 client+server même major; transports:[websocket] en prod; pas de fallback long-polling.
- (version) Stack vérifiée juillet 2026: Node LTS >=22/24; React 19.x; Vite 6.x; TypeScript 5.6+; socket.io/socket.io-client ^4.8.3 (même major, pas de breaking, déc.2025); express 5.2.1 stable (Node18+, breaking mineurs pour static+/health) — alternative 4.x latest-4 tag; zod v4.4.0 (avr.2026) breaking (.strict() déprécié→z.strictObject, .email()→z.email(), ZodError.format déprécié→z.treeifyError) — Zod3 dispo via zod/v3; zustand 5.x; vitest 2.x; web-midi-test latest (jazz-soft); shadcn/ui + Tailwind.
- (decision) AD-9 Wire format JSON compact v:1 (vs binaire). Binds: NFR-15. Prevents: overhead debuggabilité. Rule: événement channel-voice ~6 champs, JSON ~80-120B négligeable à échelle humaine (100-200 ev/s); apporte Zod+logs lisibles; binaire(MessagePack) hors MVP.
- (decision) AD-10 Auth owner par shared secret OWNER_SECRET (vs JWT). Binds: FR-2, NFR-9,10. Prevents: fuite secret dans bundle Vite. Rule: OWNER_SECRET env serveur uniquement, JAMAIS VITE_*; performer saisie manuelle token→socket.auth.token; io.use compare crypto.timingSafeEqual; erreurs génériques anti-énumération; pas de localStorage MVP; .env gitignored.
- (decision) AD-11 Scheduler listener send(data,ts) + lookahead simple (vs compensation avancée). Binds: NFR-1,5, FR-25,26. Prevents: jitter timers JS + queue infinie. Rule: target=performance.now()+LOOKAHEAD_MS(40); MIDIOutput.send driver-level anti-jitter; MAX_LATE_MS=200→fallback immédiat noteOn/noteOff, drop CC HF; BUFFER_CAP=256 drop oldest+warning local; pas de re-loging; srvTs ajouté pour télémétrie seulement.
- (decision) AD-12 Remappage forcé canal listener. Binds: FR-13,15. Prevents: listener multi-timbral entendant fusionné vs séparé — comportement attendu single-timbral. Rule: tout event reçu rerouté vers canal listener choisi (0-15 data, 1-16 UI, conversion -1 à l'edge) avant send; canal d'origine performer remplacé.
- (decision) AD-13 Rate limiting per-socket token bucket. Binds: FR-22, NFR-3. Prevents: contournement via frames WS (limiteurs HTTP ne voient pas WS). Rule: socket.use token bucket capacity 200 burst, refill 100/s par performer; dépassement→rate:limited+log échantillonné; listener n'émet jamais midi:event.
- (decision) AD-14 Mock MIDI Output à chaud. Binds: FR-12, NFR-19. Prevents: bloquer pipeline sans IAC/Dexed. Rule: MockMidiOutput implémente {send(bytes,ts)} visualise bytes; sélectionnable dropdown sortie='Mock / Debug'; switch Mock à chaud autorisé après port réel; pipeline socket→scheduler→encode testable CI+démo.
- (decision) AD-15 Origin allowlist + mono-domaine (anti-CSWSH). Binds: FR-23, NFR-13. Prevents: cross-site WebSocket hijacking + CORS preflight. Rule: origin: process.env.PUBLIC_ORIGIN au upgrade HTTP; zéro CORS (single origin); HTTPS obligatoire secure context Web MIDI.
- (decision) AD-16 Déconnexion listener après 3 forbidden (pas de ban persistant). Binds: FR-19, Q-UX1. Prevents: listener flood tente midi:event. Rule: 3 tentatives forbidden→serveur déconnecte; pas de ban persistant MVP; message UI 'Connexion interrompue: action non autorisée.'
- (decision) AD-17 Fail-safe musical déconnexion. Binds: FR-24. Prevents: notes orphelines à déconnexion listener/perte sortie. Rule: scheduler arrête envoi à déconnexion/perte port; reconnexion→reprise flux live sans re-loger passé (pas de replay).
- (decision) AD-18 Logs structurés échantillonnés. Binds: FR-30. Prevents: flood log par midi:event. Rule: pas de log par event; logger connexions, room changes, erreurs validation échantillonnées 1/N avec seq+raison, rate-limit compteur agrégé+flush, panic; LOG_MIDI=1 debug dev.
- (decision) AD-19 Tests: unitaires 100% + intégration in-process + validation manuelle. Binds: NFR-16,17,18. Prevents: régression mapping/panic/scheduler/schema/registry/ratelimit. Rule: Vitest+jsdom+web-midi-test mock requestMIDIAccess; Socket.IO in-process; Mock MIDI Output CI; plan manuel IAC→Dexed→MIDI Monitor 11 étapes; grep build zéro-secret.
- (decision) AD-20 Déploiement HTTPS mono-domaine. Binds: NFR-8,13. Prevents: secure context fragmenté. Rule: mono-process Express sert static Vite + Socket.IO même origin; Caddy auto-TLS ou host managé (Render/Fly.io); /health {ok,uptime,ownerActive:bool,listeners:n}; graceful shutdown drain+close io; env PORT/OWNER_SECRET/PUBLIC_ORIGIN/LOG_MIDI/MAX_LISTENERS.
- (question) Q-ARCH-1 (scaffolding): Zod 3 (zod/v3, .strict() stable) vs Zod 4 (z.strictObject(), breaking). Recommandation: pin Zod 3 ^3.23 pour stabilité MVP, migrer v4 post-traction. À figer au scaffolding via pnpm outdated.
- (question) Q-ARCH-2 (scaffolding): Express 4.x (latest-4 tag mai2026) vs 5.2.1. Recommandation: Express 5.2.1 (Node18+, mature déc.2025) — breaking changes mineurs pour static+/health. À figer au scaffolding.
- (question) Q-ARCH-3: Tuning MAX_LATE_MS(200)/LOOKAHEAD_MS(40) en conditions réelles — politique figée, calibrage ouvert (hérité PRD Q-1/Q-6).
- (question) Q-ARCH-4: Dépendance directionnelle stricte app→features→entities→shared→lib enforce par eslint-plugin-bound-modules ou convention revue? Recommandation: plugin eslint pour enforce automatique.
- (question) Q-ARCH-5: Build @fmlw/shared via tsc (MVP) vs tsup dual ESM/CJS. Recommandation: tsc suffit (consommé ESM des deux côtés).
- (event) spine finalized: ARCHITECTURE-SPINE.md (AD-1..AD-20, status final) + ARCHITECTURE.md (doc humaine FR) + 8 ADR (ADR-0001..ADR-0008). Versions web-vérifiées juil.2026 (Zod v4.4.0 breaking, Express 5.2.1, Socket.IO 4.8.3). Reviewer gate inline: lens1 versions OK, lens2 adversarial divergence — aucun trou non-fermable (channel edge conversion, srvTs naming, MockMidiOutput/RelayService interfaces, panic independence tous fixés en conventions/AD).
- (decision) Q-ARCH-1 RESOLU (validation Zub 2026-07-06): pin Zod 3 ^3.23 pour le MVP (.strict() stable). Migration Zod 4 (z.strictObject) eventuelle post-traction. Binds: AD-5. Prevents: instabilite API Zod 4 breaking au scaffolding.
- (decision) Q-ARCH-2 RESOLU (validation Zub 2026-07-06): Express 5.2.1 pour le MVP (Node18+, mature dec.2025, breaking mineurs pour static+/health). Binds: AD-1, AD-20.
- (decision) Q-ARCH-3 RESOLU (validation Zub 2026-07-06): LOOKAHEAD_MS=40 et MAX_LATE_MS=200 conserves par defaut; valeurs tunables apres tests reels. Politique fallback/drop par type figee. Binds: AD-11.
- (decision) Q-ARCH-4 RESOLU (validation Zub 2026-07-06): enforce des dependances directionnelles (app→features→entities→shared→lib; handler→services→shared) via plugin ESLint adapte (ex. eslint-plugin-bound-modules), pas seulement revue humaine. Binds: conventions dependances.
- (decision) Q-ARCH-5 RESOLU (validation Zub 2026-07-06): build @fmlw/shared avec tsc pour le MVP (consomme ESM des deux cotes). Pas de tsup au depart. Binds: AD-5, structural seed.
- (event) Architecture validee par Zub 2026-07-06. Q-ARCH-1..5 toutes resolues et integrees aux documents. Spine + doc gardent status final. Prochaine etape: bmad-create-epics-and-stories (a lancer explicitement, pas maintenant).
